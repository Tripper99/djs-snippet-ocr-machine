# TODO

## Ideas for future improvements

- Show a thumbnail of the pasted image alongside its OCR text in the card
- Persist snippets between sessions (save/load to a file)
- Keyboard shortcut to paste directly into the app (Cmd+V when window is focused)
- Option to strip hyphenation (join words split with "-" across lines)
